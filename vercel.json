{
	"version": 2,
	"framework": "vite",
	"buildCommand": "npm run build",
	"outputDirectory": "build",
	"routes": [
		{ "handle": "filesystem" },
		{
			"src": "/.*",
			"dest": "/index.html"
		}
	]
}
